<%= link_to "Add User", new_path, class: "btn btn-lg btn-primary"%>

<% provide(:title, 'All users') %>
<%= will_paginate @users %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>
