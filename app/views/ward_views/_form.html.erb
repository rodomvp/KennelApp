<%= form_for(@ward_view) do |f| %>
  <% if @ward_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ward_view.errors.count, "error") %> prohibited this ward_view from being saved:</h2>

      <ul>
      <% @ward_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
